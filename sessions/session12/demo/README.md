# 🧩 JavaScript Basics — Core Concepts

## 📑 Index

- **Script Loading**

  - `<script>` tag
  - Script in the `<head>`
  - External JavaScript files
  - `defer` attribute to delay execution until the DOM is ready

- **Variables**

  - `const`, `let`, `var`
  - Best practice:
    - Use `const` by default
    - Use `let` only if reassignment is required
    - Avoid `var` (legacy, problematic scope)

- **Data Types**

  - Number
  - String
  - Boolean
  - Arrays
  - Objects

- **Variable Scope**

  - Global scope
  - Block scope (`{ }`)
  - `const` and `let` are block-scoped
  - `var` is function-scoped (ignores block scope)

- **Arrays**

  - Array literals `[]`
  - Access by index
  - Iteration patterns
  - Common use cases: lists, collections, histories

- **Object Literals**

  - Object literals `{}` to group related data
  - Objects should always be declared with `const`
  - Object properties can be modified without reassigning the object

- **Conditions**

  - `if / else`
  - `else if`
  - `switch` statement

- **Loops**

  - `while` loop
  - `do...while` loop
  - `for` loop
  - `for...of` loop

- **Built-in Objects**

  - `Math`
    - `Math.random()`
    - `Math.floor()` (rounds down, removes decimals)
  - `Date`
    - Creating date objects
    - Working with timestamps
    - Formatting dates

- **Console & Basic Interaction**
  - `console.log()` for output and debugging
  - `prompt()` for basic user input
  - Console-based execution (no UI)

---

## 🚀 Advanced

- Combine arrays and objects to represent structured data.
- Proper scope management prevents bugs and side effects.
- These fundamentals are the basis for DOM manipulation.

> [!TIP]
> A solid understanding of **scope, arrays, and objects** is essential before moving on to more advanced JavaScript topics.
